## Packages
framer-motion | Smooth transitions between game states and animations for feedback
canvas-confetti | Celebration effect for high scores
@types/canvas-confetti | Types for confetti
lucide-react | Icons for UI elements (already in base, but listing for completeness)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
